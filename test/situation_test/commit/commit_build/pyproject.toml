[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "calculator"
version = "1.0.0"
description = "Calculatrice en ligne de commande en Python"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
calc = "calculator:main"
